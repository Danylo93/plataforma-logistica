// import { InAppBrowser } from 'react-native-inappbrowser-reborn';

export const openInAppBrowser = async () => {
  // Todo: O que esse codigo deveria fazer?
  console.log(
    'Esse codigo foi comentado pois a lib react-native-inappbrowser-reborn não esta incluida no package.json',
  )
  // try {
  //     const url = 'https://google.com';
  //     const result = await InAppBrowser.open(url, {});
  //     console.log(result);
  // } catch (error) {
  //     console.error(error);
  // }
  //
  // return <Button title="Abrir InAppBrowser" onPress={openInAppBrowser} />;
}
